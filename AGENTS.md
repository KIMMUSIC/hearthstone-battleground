# 개발 에이전트 지침

## 우선 목표

학습이 실행되는 것보다 게임 규칙의 정확성, 관측·액션 계약, 실험 재현성을 먼저 완성한다. 구체적인 단계와 통과 조건은 `docs/DEVELOPMENT.md`를 따른다.

명확하고 되돌릴 수 있는 로컬 구현·테스트·실험 운영 작업은 자율적으로 완료한다. 장시간 학습은 로컬 실행본, 전용 환경, 자원 한도, 중단 조건을 확인한 뒤 시작한다. 사용자가 이미 정한 범위와 한도는 다시 확인받지 않는다.

## 역할과 변경 소유권

- Codex가 소스 코드, 규칙, 보상, 테스트, 실험 설정의 변경을 책임진다.
- Codex가 사용자의 로컬 PC에서 확정된 버전과 명세를 실행하고 지정된 `runs/<run_id>/`에 결과를 기록한다. 기본 실행 경로는 WSL Ubuntu의 Linux 감독기와 전용 Python 3.11 가상환경이다.
- 과거 Grok Bot 실행·응답 문서는 이력 근거로만 보존한다. 현재 작업은 Grok Bot 응답을 기다리거나 Grok Bot에 새 학습을 맡기지 않는다.
- 개발 checkout과 학습 실행본을 분리한다. 진행 중인 실험의 소스를 수정하지 않는다.
- 독립적인 하위 작업에 도움이 될 때만 Codex native subagent를 사용한다. 쓰기 범위를 명확히 나누고 결과 통합과 최종 검증은 주 에이전트가 담당한다.

## 기존 프로젝트 취급

- `D:\HearthStoneAI`는 읽기 전용 참고 자료이며 수정·이동·삭제하지 않는다.
- 기존 버그는 회귀 테스트 사례로 활용한다. 과거 WORK 문서의 성공 주장을 현재 검증으로 간주하지 않는다.
- 기존 카드 DB의 `rl_id`를 새 모델의 영구 식별자로 사용하지 않는다.
- 기존 모델은 참고용이며 새 환경은 재학습을 기본으로 한다. 가중치 이전은 명시적인 매핑·규격 호환 검증이 있을 때만 별도 실험으로 수행한다.

## 규칙과 데이터

- 기준 패치, 카드 목록, 규칙 출처, 지원 효과를 문서화한다. 게임 규칙을 기억에 의존해 확정하지 않는다.
- 미구현 효과가 있는 카드는 학습 풀에서 제외한다. 공통 버프로 대체하지 않는다.
- 엔진의 카드 식별자는 안정적인 원본 ID를 사용하고, 임베딩 인덱스 매핑은 버전이 있는 별도 파일로 저장한다.
- 카드 추가·삭제 때 기존 인덱스를 다른 카드에 재사용하지 않는다. 빈 슬롯과 알 수 없는 카드를 구분하고, 등록되지 않은 카드는 명시적으로 오류 처리한다.
- 학습 시작 시 데이터·매핑·관측·액션 규격의 호환성을 검사한다. 모델 로드가 된다는 사실만으로 호환된다고 판단하지 않는다.
- 모델에 카드 DB·매핑·설정의 해시와 코드 버전을 함께 기록한다. 서로 다른 사본을 수동 동기화하는 방식에 의존하지 않는다.

## 엔진과 인터페이스

- 게임 엔진은 UI·네트워크·학습 라이브러리와 분리한다. Python/C# 유지 여부는 단계 0에서 결정하며 기존 gRPC 구조를 의무적으로 계승하지 않는다.
- 네트워크를 사용하면 명시적인 세션 ID, seed, 프로토콜 버전, 종료 이유를 전달한다. 연결 주소를 게임 ID로 사용하지 않는다.
- 액션 인코딩·디코딩·마스킹은 하나의 명시적 규격을 공유한다. 알 수 없는 액션을 END_TURN으로 바꾸지 않는다.
- 손패·보드·상점 용량을 모든 경계에서 일치시킨다. 범위를 넘는 카드를 조용히 잘라내지 않는다.
- 관측에는 황금 여부, 남은 행동·스왑 수처럼 구현상 결과를 바꾸는 공개 상태를 포함한다. 실제로 숨겨진 상대 정보는 유출하지 않는다.
- 종료와 시간 제한 중단을 구분한다. 죽은 상태의 HP를 포함해 모든 반환 관측이 선언된 공간을 만족해야 한다.
- 난수는 명시적으로 주입한다. 세션 간 전역 가변 RNG·상대 풀을 사용하지 않는다. 병렬 실행의 재현 조건을 문서화한다.
- 전투 이벤트 순서와 다음 공격자 선택은 사망·소환·환생에 대한 테스트로 검증한다.

## 테스트와 실험

### 결과에 따른 상세 계획 갱신

- 각 일일 goal 시작 시 직전 작업의 결과 문서와 실제 코드·테스트·실험 산출물을 확인한다. 완료 보고만으로 선행 조건 충족을 추정하지 않는다.
- 상세 계획은 고정 작업 목록이 아니다. 직전 결과에 따라 `docs/DAILY_GOALS_2026-09.md`의 현재·후속 목표, 선행 조건, 구현 범위, 검증 기준, 실험 예산과 작업 순서를 갱신한 뒤 의존 작업을 실행한다. 일정 개요가 바뀌면 `docs/PLAN_2026-09.md`도 맞춘다. 이후 새 계획 문서를 채택하면 같은 규칙을 적용한다.
- 작업 도중에도 테스트 실패, 규칙 오류, 성능 정체, 처리량 변화, 외부 실행 장애 등 후속 판단을 바꾸는 결과가 나오면 영향을 받는 계획을 즉시 재검토한다. 매일 종료 또는 인계 전에는 후속 계획의 최신 여부를 반드시 확인한다.
- 이미 완료한 작업은 반복하지 않고, 실패한 선행 작업은 후속 작업보다 먼저 배치한다. 새 결과로 불필요해진 작업은 이유를 남기고 제외하며, 추가 검증이 필요하면 해당 근거와 완료 기준을 기록한다. 달력 날짜보다 의존 관계를 우선한다.
- 완료된 작업의 기록과 당시 판정 기준은 보존한다. 불리한 결과를 통과시키려고 기존 성공 기준을 소급 변경하지 않는다. 새 기준은 새 실험에 적용하며 변경 이유와 근거를 남긴다.
- 결과 문서에는 계획 변경 전후, 근거 파일·지표, 영향을 받는 날짜, 다음 실행 목표를 기록한다. 변경이 필요 없으면 확인한 근거와 함께 '계획 유지'를 기록한다. 근거 없는 일괄 재작성은 하지 않는다.
- 재계획은 사용자가 정한 장기 목표와 권한·자원 한도를 유지한다. 계획 갱신만으로 새 비용·장시간 실행·별도 날짜의 goal 실행을 승인하거나 시작하지 않는다. 현재 목표를 실제로 완료하지 않았다면 후속 계획을 수정했더라도 완료로 보고하지 않는다.

- 발견한 결함은 먼저 실패하는 회귀 테스트로 고정한다. 테스트 실패 시 비정상 종료 코드를 반환해야 한다.
- 관측 크기 검사, 빌드 통과, 구형 환경 테스트를 현재 게임 규칙 검증의 대체로 사용하지 않는다.
- 학습 상대와 평가 상대를 분리한다. 평가는 고정된 세트로 실행하며 상대 풀에 쓰지 않는다.
- 보조 보상을 포함한 총 보상과 전투 승률·생존·피해·행동 낭비 지표를 별도로 기록한다.
- 실험마다 한 주요 변경만 비교한다. 실행 전에 평가 seed·상대·예산·판정 기준을 고정한다.
- 실행 예산과 중단 조건은 외부 설정으로 관리한다. 학습 루프에 임의의 장시간 실행을 하드코딩하지 않는다. 로컬 학습은 고유 run ID, 고정 소스 해시, 빈 출력 폴더, CPU1, 샘플링 RSS2GiB, 감독기 로그, 실행 전후 프로세스·출력 점검을 갖춘 뒤 수행한다.
- checkpoint는 중간 실패로 이전 정상 파일을 잃지 않게 저장한다. 재개 시 모델·옵티마이저·카운터·필요한 난수 상태를 복구하고 지원되는 재현 수준을 기록한다.

## 의존성·기록·완료 보고

- 기본 환경에 필요한 최소 의존성을 선정하고 버전을 고정한다. 새 프레임워크는 필요성과 대안을 짧은 설계 결정으로 남긴다.
- 모델·대용량 원본·실행 로그는 Git 소스 이력과 분리하고 위치·해시를 기록한다. 비밀 값은 저장하지 않는다.
- 변경 후 해당 동작 테스트와 적용 가능한 lint·typecheck·build를 실행한다. 실행하지 못한 검증은 명시한다.
- 최종 보고에는 변경 파일, 동작 변화, 검증 결과, 남은 제한을 포함한다. 현재 단계 통과와 프로젝트 전체 완성을 구분한다.
- 커밋할 때는 변경 이유를 제목으로 쓰고, 필요한 경우 `Constraint:`, `Rejected:`, `Confidence:`, `Scope-risk:`, `Tested:`, `Not-tested:` 등 Lore trailer를 사용한다.
