# 2026-09-07 — 학습 상대 구성과 추첨 구현

상태: **완료**. 9/10–15 연속 goal의 선행 복구로 실제 2026-09-06 KST에 수행했다.

## 변경과 검증

`training._config` → `train` → `BgEnv` → `Game`에 `opponent_mode`를 전달한다. 기본 fixed-v1은 기존 상대로 유지하고 diverse-v1은 42467/72387을 독립 opponent RNG로 뽑아 8턴 일정을 만든다. 티어·턴별 카드 수는 유지한다. 관측·37행동·보상은 변경하지 않았다.

manifest/checkpoint에 분포 명세와 모드를 기록한다. 학습 재개는 모드 불일치를 거부하고, 고정 평가의 명시적 opponents 경로는 허용한다. 이 평가 경로는 학습용 fixed 카드의 불필요한 존재 검사를 요구하지 않는다. 잘못된 문자열/타입·누락 ID·잘못된 티어는 오류로 처리한다.

구형 snapshot003 ZIP의 소스를 임시 폴더에서 실행해 3개 seed의 전체 에피소드 공통 상태·전투 결과가 일치함을 확인했다. 추가 검증은 슬롯 추첨 순서와 실제 전투 info, 세션 reset/step 교차 독립성, 상대 생성 전후 shop/combat RNG와 entity ID 불변, 평가 입력 읽기 차단, 관측·마스크, metadata와 재개 거부를 포함한다.

검증 명령(구현 담당 실행 출력 확인):

- `python -m pytest tests/test_game.py tests/test_env.py tests/test_training.py` → 43 passed.
- `python -m pytest` → 93 passed, 6 skipped. 6건은 Windows에서 실행 불가한 Linux 감독기 검사이며 클라우드 smoke에서 수행할 예정이다.
- `python -m ruff check src tests` → 통과.

변경 파일: `src/hearthstone_ai/game.py`, `env.py`, `training.py`, `tests/test_game.py`, `test_env.py`, `test_training.py`. 실제 실험 학습은 미실행이며 일부 단위 테스트의 짧은 저장·재개 검증은 본 학습 성과로 계산하지 않는다.

새 source SHA256: `81b0dae34dfbac6bf875d1ebcf2647f34e68817b1b0987bc63685801e614cd90`. 구형 metadata/모델은 변경하지 않았다.

## 계획 갱신과 다음 단계

9/8에는 이 소스를 고정한 새 checkout을 전달해 Linux 테스트와 두 모드64스텝 smoke를 실행한다. 9/9 본 비교는 새 소스에서 다시 초기화한 6개 모델로 수행한다. 9/10 평가는 009 명세의2,000게임을 유지한다. campaign 감사 도구는 실제 기존 raw report 구조에 대한 검증을 완료한 뒤 패키지에 고정한다.
