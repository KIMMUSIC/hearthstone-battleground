# 2026-09-16 — 첫 카드 묶음과 안정적인 매핑

상태: **완료**. 실제 수행일 2026-09-06. 선행 9/15의 사망·소환 계약과 121개 테스트 통과 기록을 확인했다. 별도 학습 실험은 실행하지 않았다.

## 계획 반영

첫 묶음은 일반 허수아비골렘 96763(2티어, 2/3)과 소환 토큰 96764(1티어, 2/1)로 한정한다. 골렘 황금과 뱃사람 즉시공격은 출처·처리 순서 검증이 충분하지 않아 보류를 유지한다. 상점 카드는 4종에서 5종으로, 전체 등록 ID는 6개로 늘어난다. 10–20종 목표 때문에 미지원 카드를 추가하지 않는다.

골렘이 정상 구매 경로에서 자동 트리플되어 미지원 황금 전투를 일으키지 않도록, 골렘·토큰은 황금과 트리플을 제외한다. 골렘 3장은 일반 상태로 남는다. 이 제한은 실제 게임 규칙이 아닌 연구용 부분 규격이다. 기존 4종의 황금·트리플·발견은 유지한다.

## 데이터와 호환성 계약

- 카드 규격 `archival-subset-v2`, 매핑 내용 버전 `card-mapping-v2`. JSON 구조 스키마는 기존 1을 유지한다.
- 기존 ID의 인덱스 1–4를 보존하고 96763→5, 96764→6을 추가한다. 패딩은 0이다.
- 관측 `onehot-slots-v2`: 슬롯 폭 11→13. 위치·상태 필드와 행동 37개는 유지한다.
- 토큰은 소환 가능하지만 상점·발견·일반 상대 풀에서는 제외한다.
- 기존 009 실행본·모델·평가 파일은 보존한다. 새 계약에서는 구형 checkpoint를 로드하기 전에 거절한다.
- 근거: [9/13 카드 출처](2026-09-13-card-evidence.json), [사망·소환 계약](../DEATH_SUMMON_CONTRACT.md), [규칙 계약](../RULES.md).

## 검증 결과

- WSL Ubuntu / `/home/hwa3060/hsai-local/venv311/bin/python`에서 `python -B -m pytest -q -p no:cacheprovider`: **128 passed in 17.61s**, 종료 코드 0.
- `ruff check src scripts tests`: **All checks passed!**, 종료 코드 0.
- [200게임 통합 결과와 최종 서명](2026-09-16-integration.json): seed 0–99 각각 heuristic/random으로 총 9,326행동, 모두 8턴 horizon 종료. 골렘 구매 160회·플레이 148회 확인. 발견 획득도 있으므로 구매 수와 플레이 수는 같지 않다. 상점·발견 토큰 제외, 관측 공간, 미지원 황금 부재를 매 행동 확인했다. 이는 정상 진행 검증이며 성능 향상 주장이 아니다.
- 실제 009 fixed seed 7 / 8,192스텝 checkpoint는 `Checkpoint compatibility mismatch`로 거절됐다. 모델 로드·재학습으로 우회하지 않았다.
- 신규 회귀: 기존 인덱스 보존, 토큰 샘플 제외, 황금 생성 거부, 모순된 트리플/황금 설정 거부, 골렘 트리플 제외, 명시 상대 토큰 거부, 휴리스틱의 미지원 트리플 보너스 제외, 기본 카탈로그 사망 소환, 골렘·토큰·빈칸의 관측 구분과 미등록 ID 거부.
- 기존 snapshot003 비교는 당시 카드 데이터로 현재 엔진을 실행해 3개 seed 전체 trace를 비교하도록 유지했다. 새 카드 풀의 상점 분포를 과거와 같다고 주장하지 않는다.
- 독립 코드 검토에서 완료를 막는 결함은 발견되지 않았다. 검토자는 compileall도 통과 확인했다. 별도 pyright/LSP 진단은 도구가 없어 실행하지 않았으며 정적 검증 근거는 Ruff와 compileall이다.

## 변경 파일

데이터는 `data/cards.json`, `data/card_mapping.json`; 구현은 `src/hearthstone_ai/cards.py`, `game.py`, `evaluation.py`, `artifacts.py`; 회귀는 `tests/test_cards.py`, `test_game.py`, `test_evaluation.py`, `test_env.py`, `test_death_summon.py`다. README, 규칙·연구 계약·개발 단계·일일 계획·월간 계획을 현재 범위에 맞췄다. 원본 프로젝트와 기존 실행 산출물은 수정하지 않았다.

## 후속 계획

9/17은 추가 후보의 효과·황금·생성 경로 검증과 관측·행동 계약 점검이다. 10–20종은 조건부 목표로 유지하며, 이번 묶음의 미지원 효과를 먼저 검토한다. 9/18 경제 경계 검사, 9/19 별도 실행본의 짧은 학습 측정 순서는 유지한다. 이번에는 9/17 이후 작업이나 새 학습을 시작하지 않는다.
