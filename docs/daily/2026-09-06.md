# 2026-09-06 — 기준선과 비교 명세 고정

상태: **완료**. 실제 점검: 2026-09-06 03:20–03:24 KST. 이 날은 명세·기준선·환경 확인 목표이며 학습 실행 목표가 아니다.

## 목표와 실제 작업 범위

직전 실험의 모델·설정·원본을 확인하고 상대 구성 다양화 비교의 입력·인터페이스·평가·판정 기준을 고정한다. 기존 Bot의 현재 환경과 진행 작업을 확인한다. 오늘은 학습·평가 실행이나 9/7 구현을 시작하지 않는다.

선행 근거: [holdout008 보고서](../HOLDOUT_RESULT_008.md), [실제 입력·모델 목록](2026-09-06-inventory.json), 프로젝트 소스 직접 확인과 독립 읽기 전용 검토.

## 완료한 작업

- 로컬006/007/008 반환 ZIP의 CRC와 SHA256을 확인했다. 여섯 최종 모델의 내부 JSON에서 timestep=8,192, epoch1 update=256 / epoch4 update=1,024를 확인했다. 모델 pickle을 역직렬화하지 않았다.
- 현재 모든 엔진 소스 파일의 해시로 서명을 재계산해 frozen003 서명 `80b5ca7a81687165553578ef28f581025d754a6759a04f42ae8d00387c5b97a8`과 일치함을 확인했다. 엔진·모델·기존 결과를 수정하지 않았다.
- `research_train.json` 및 카드·매핑·규칙·평가 입력 해시, 여섯 모델의 archive member·config·compatibility를 JSON으로 남겼다.
- [비교 명세009](../../experiments/OPPONENT_DIVERSITY_009.md)를 작성했다. 기존 고정 상대와 1티어 두 카드 균등 구성의 비교이며 상대 티어·카드 수는 유지한다.
- 기존 평가 조건은 개발용으로 표시하고 새 개발 seed 401–420을 예약했다. 신규 학습 6회 49,152스텝, 초기/최종과 기준선 포함 평가 2,000게임을 사전 명세했다. 오늘 실제 추가 학습·평가 **0**.

## Grok Bot 현재 상태 확인

대상: 기존 `BG Training Operator` 대화. 이전 마지막 실험은 holdout008 완료였고 9/6 03:20 KST에 읽기 전용 점검 요청을 전송했다. 첫 답변은 03:21:54 KST에 도착했다. [관측한 응답 텍스트](2026-09-06-grok-resource-ui.txt)를 보존했다. 이는 Bot의 조회 명령·출력 보고이며 Codex가 cloud shell에 직접 접근해 얻은 출력은 아니다.

| 항목 | 관측된 보고 |
| --- | --- |
| 조회 시각 | 2026-09-05T18:20:58Z 및 18:21:43Z |
| OS | Debian GNU/Linux 13, x86_64 |
| CPU | affinity 0–7, cgroup cpu.max `800000 100000`; nproc는 1 |
| 메모리 | cgroup 최대 17,179,869,184bytes, 사용 8,764,493,824bytes; free의 available 4,843,204,608bytes |
| 디스크 | /workspace 가용 116,058,501,120bytes |
| GPU | nvidia-smi 미설치/사용 불가, /dev/nvidia* 없음; 사용 가능한 NVIDIA GPU 증거 없음 |
| Python | 기존 venv311 Python 3.11.16 |
| 패키지 | NumPy2.4.2 / Torch2.10.0+cpu / Gym1.2.3 / SB3·contrib2.7.1 / pytest9.0.2 / Ruff0.15.12 |
| 기존 경로 | snapshot003, curve006, epoch007, holdout008 존재 |
| 소스 무결성 | SNAPSHOT_FILES 44개 중 44개 일치, fail0/missing0 |
| 서비스 보장·재시작 지속성 | unknown |

CPU quota는 처리량 보장이 아니다. nproc=1의 원인은 확인하지 않았으며 환경변수 영향이라는 Bot 추정은 확정 사실로 취급하지 않는다. available 메모리는 약 4.51GiB, 디스크는 약 108.09GiB이며 Bot의 GB/GiB 혼용을 그대로 인용하지 않는다. 다음 실행에서는 다시 측정한다.

첫 /proc 점검은 hit_count0을 보고했지만 명령어의 절대 경로 기반 필터만으로는 상대 경로 실행이 누락될 수 있어 cwd를 포함한 재검사를 요청했다. 점검 중 Bot이 임시 검사 파일을 만들었다고 보고했으므로 '파일 쓰기가 전혀 없었다'고 주장하지 않는다. 프로젝트 코드·학습 결과 변경은 요청하지 않았다.

보완 응답: [프로세스 점검 근거](2026-09-06-grok-process-ui.txt). 2026-09-05T18:24:04Z의 실제 `/proc` 조회에서 프로젝트 cwd 또는 절대 경로+Python/작업 이름을 검사했다. 자기 PID2744625와 부모2744610을 제외했고 hit_count=0, permission_denied_count=3이었다. **읽을 수 있는 범위에서 관련 실행을 발견하지 못했다**는 결론이며 접근 거부 항목까지 포함한 전역 부재 증명은 아니다. 권한 변경은 하지 않는다. 실제 학습 전에는 동일 사용자 작업 상태와 프로세스를 다시 확인한다.

점검 실행 명령은 `/workspace/hsai-rebuild/snapshot-001/venv311/bin/python /workspace/hsai-rebuild/_status_scan_pids_v2.py`였다. Bot은 v1 임시 파일 `_status_scan_pids.py`를 생성 후 삭제했고 v2는 남아 있다고 보고했다. 이 삭제는 이번 요청의 삭제 금지에 어긋난다. 이를 읽기 전용 운영 성공으로 숨기지 않는다. 상태 점검 결과는 제한을 명시해 활용하되, 후속 점검은 Codex가 제공한 고정 진입점과 출력 경로만 사용하도록 보완한다. 기존 학습·평가 재시작은 없다고 보고됐으며 새 모델/학습 출력도 요청하지 않았다.

## 검증과 판정

실행한 로컬 검증은 Python 표준 라이브러리의 zipfile CRC 검사, SHA256 재계산, model.zip/data JSON 카운터 및 metadata 읽기, 입력 파일 해시 확인이다. 종료 코드 0. 출력을 `2026-09-06-inventory.json`에 기록했다.

기존1040게임의 성능 감사는 원본 해시와 기존 감사 파일을 연결해 확인했다. 이번 목표에서 기존 학습·평가를 재실행하지 않았다. 코드 변경이 없어 전체 학습 테스트를 반복하지 않는다.

## 계획 갱신

- 9/7의 추상적인 '상대 다양화' → 모드 설정 전달, 상대 전용 RNG, reset 시 8턴 생성, 평가와 재개 계약의 구체적인 구현으로 변경했다. 근거: 현재 `_config`는 추가 항목을 거부하고 `Game`은 고정 템플릿만 선택한다.
- 9/8에는 기존003 소스/환경 보존과 새 실행본 smoke 뒤 실제 실행 상한·해시를 고정하도록 한다. 추가로 점검 스크립트도 제공한 진입점만 사용하고, 임시 파일 삭제·검사 실패 자동 정리를 금지한다. 근거: 이번 Bot의 임시 파일 삭제 보고.
- 9/9의 6회 신규 학습은 유지한다. 9/10 평가는 초기 모델까지 포함한 2,000게임으로 명시하고 상대 조건별 작업 분리와 디스크 산정을 추가한다.
- 9/11 이후 큰 순서는 유지한다. 정책 개선이 보장되지 않으므로 실제009 결과에 따라 다시 갱신한다.
- 구형 모델 호환성 검사 우회, 보상 변경, 2티어 상대 추가, 다음 날짜 목표 자동 실행은 하지 않는다.

## 완료 대조와 다음 재개 지점

| 요구사항 | 확인 근거 |
| --- | --- |
| 최신 코드·설정·모델 확인 | 입력 목록의 소스·입력·ZIP·모델 해시와 실제 카운터 |
| 본 평가 세트 개발용 분류 | 명세009의 평가 세트 분류와 새 개발 seed 예약 |
| 분포·출처·추첨·seed·관측 정의 | 명세009의 두 조건과 9/7 구현 계약 |
| 동일 새 소스·3seed·8192·epoch4 비교 | 명세009 학습 명세, 총49,152스텝 |
| Bot 현재 환경·작업 점검 | 현재 자원 응답, cwd 포함 프로세스 코드·출력과 접근 제한 |
| 학습 미시작·기존 실험 미반복 | 오늘 요청은 점검만, 로컬은 archive/JSON 읽기, 모델/소스 해시 유지 |
| 결과·재개 기록과 계획 갱신 | 이 문서와 갱신된 일일 계획·일정 개요 |

오늘 목표의 추가 실행 작업은 없다. 미확정 사항은 장기 서비스/지속성, 접근 거부 프로세스 내용, 아직 구현하지 않은009 실행본 해시와 smoke 기반 예산이며 해당 범위를 명시했다. 후자는 9/8 실행 전 게이트이고 오늘의 명세 작성 누락이 아니다.

다음 goal은 **9/7**이다. 비교 명세009의 `opponent_mode` 전달·독립 RNG·평가/재개 계약과 필수 검증부터 구현한다. 9/9 본 학습을 앞당겨 시작하지 않는다.
