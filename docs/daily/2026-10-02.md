# 2026-10-02 — 8인 학습 인터페이스와 짧은 실행

상태: **완료**. 실제 수행일2026-09-06. 실행 ID `lobby-032-local-r1`.

10/1의 제한된 8인 로비 검증 위에 Gymnasium 어댑터와 전용 학습 경로를 추가했다. 학습 좌석은0, 나머지7좌석은 deterministic heuristic 정책이다. 관측은 기존 `LobbyObservation`의 자기 상태와 공개 상태만 사용하고, 37개 합법 행동 마스크를 함께 제공한다. 학습자의 순위가 확정되면 에피소드를 종료하며 보상은 `reward=(4.5-rank)/3.5` 한 번만 지급한다. 제한 중단으로 순위가 없으면 보상0이며 가짜 순위는 만들지 않는다.

WSL Ubuntu 전용 venv에서 고정 실행본 `lobby-032-local-r1`을 감독기로 실행했다. 학습은 최대치인8,192 learner transition을 완료했고 실제 업데이트는1,024회다. 학습 시간은13.39초, 처리량은611.62 learner step/s, 감독 실행은 train/evaluate 합계25.76초였다. 최고 샘플링 RSS는486,748,160바이트, 출력+반환은30,496,989바이트로 한도 안이다. 학습 중 기록된 완료 에피소드는138개이고 모두 rank8, 제한 중단0개였다.

초기 모델·최종 모델·heuristic 기준선을 평가 seed20001..20020으로 각각20에피소드 평가했다. 최종 모델은 평균 순위8.0, 평균 보상-1.0, 상위4위0%로 초기 모델과 같았다. heuristic 기준선은 평균 순위4.25, 평균 보상0.0714285714, 상위4위50%였다. 따라서 이번 결과는 **학습 경로가 실제 작동한다는 증거**이며, 8인 정책 성능 개선 증거는 아니다. 예약 최종 평가 세트30001..30100은 사용하지 않았다.

감사는 checkpoint 카운터와 optimizer 포함 여부, 초기/최종 정책 변화, 모델 재로드, 평가 모델 해시 불변, 최종 모델2seed 재현, 60개 평가 action trace 전체 재생, 합법 행동·관측 공간·카드 보존, archive CRC·해시를 확인했다. [감사 JSON](../../runs/lobby-032-local-r1/audit.json), [학습 상태](../../runs/lobby-032-local-r1/outputs/train/status.json), [평가 요약](../../runs/lobby-032-local-r1/outputs/evaluate/summary.json), [후속 계획](../PLAN_AFTER_2026-10-02.md).

계획 변경: 10/3은 4주 산출물 감사와 후속 실험 기준 고정으로 유지한다. 다만 10/4 이후는 카드 확장이나 학습량 증액보다 먼저 rank8 고착 원인을 행동 trace·희소 보상·관측 스케일·좌석 편향으로 나누어 진단한다. 원인 가설과 반증 조건 없이 10만/100만 스텝 확대를 시작하지 않는다.

최종 주 에이전트 검증: WSL 전체 **305 passed in 18.35s**, `ruff check src scripts tests` 통과. 실제 어댑터의 상대 비공개 보드 변경·관측 복사·교차 세션 RNG·암묵적 학습 seed 범위와 SB3 시간 제한 중단 집계를 추가 검증했다. 현재 소스18파일은 실행본과 일치한다. 모델 서명에 포함되지 않는 테스트와 감사 도구를 실행 후 보강했으며 학습을 반복하지 않았다.

[독립 감사](../../runs/lobby-032-local-r1/audit-independent.json)는 worker CPU설정, 설정 일치,138개 학습 seed 범위, checkpoint 로드와 실제 정책 행동 일치까지 다시 확인했다. 60평가의5,848학습자 행동을 모두 재현했다. 원본 audit.json과 단계별 반환 ZIP은 보존했다. 입력 ZIP SHA256은 `10f5a72850edaa00ab0abedb30faf52668f50aaccad0c11eaae559fde56ac582`다. `lobby-032-review-probe`는 실행하지 않은 패키징 검사본이다.

[행동 분석](../../experiments/diagnostics/lobby032_action_summary.json): 최종 모델은20평가에서2,880행동 모두 동결이며 구매·배치0회다. 초기 정책은732행동 중 구매170·배치133·판매113회였다. 동일8위라는 순위만으로 두 정책의 행동 품질이 같다고 판단할 수 없다. 후속10/4는 종국 순위 신호와 상대 난이도를 우선 진단한다.
