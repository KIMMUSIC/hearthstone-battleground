# 2026-09-15 — 일반 골렘 사망 소환

상태: **완료**. 실제 수행일 2026-09-06. 선행 9/14 사망/커서 처리 검증 완료.

## 구현

명시 테스트 카탈로그의 일반 허수아비골렘96763이 사망하면 손상된 골렘96764(2/1)를 해당 위치에 소환한다. 사망을 모두 제거한 뒤 소환하고, 최대7칸·고유 entity ID·다음 공격자 순서를 지킨다. 소환·초과 생략·후속 사망을 기록한다.

Card의 선택적 summon 죽메는 정확한 type/token_id/count 필드만 받는다. count는1–7이며 없는 토큰, 잘못된 자료형, 미지원 modifier는 거절한다. 황금 죽메를 일반 효과로 대체하지 않는다. 기본4종 cards.json·매핑·상점 풀은 기존과 바이트 단위 동일하다.

## 검증 근거

- 구현 전 새 회귀9개 실패 → 구현 후 통과.
- 중간 슬롯 소환, 양쪽 동시 사망, 공격자/방어자 커서, 다중 소환과7칸 초과, 입력불변/seed재현, 후속 토큰 사망 검증.
- 합성 자기소환 연쇄가10000공격에서 combat_limit으로 종료됨을 확인.
- 미지원 immediate_attack 설정을 무시하는 결함은 별도 실패 테스트로 재현 후 exact-key 검증으로 수정.
- [대표 전투 trace](2026-09-15-combat-trace.json): 동시 골렘 사망→양쪽 토큰→후속 토큰 전투.
- 무효과1000보드 이전 버전과 동일. 독립 코드 검토 추가 결함 없음.

## 계획 갱신과 재개

최종 전체 검증은 WSL Ubuntu의 전용 Python 3.11 환경에서 수행했다. `python -B -m pytest -q -p no:cacheprovider` 결과 **121 passed in 15.85s**, `ruff check src scripts tests` 결과 **All checks passed!**, 두 명령 모두 종료 코드 0이다. 실행 위치는 `/mnt/d/HearthStoneAI-Rebuild`, Python은 `/home/hwa3060/hsai-local/venv311/bin/python`이다. 앞선 Windows 검증은 112 passed / Linux 전용 6 skipped였으며, 이후 추가 회귀를 포함한 최종 전체 판정은 WSL 결과를 따른다.

맡긴9/9 선행 복구 및9/10–15 연속 범위를 완료했다. 다음은9/16 카드 등록·매핑·황금 호환성 검증이며 이번에 자동 착수하지 않는다. 일반골렘은 전투 테스트 범위에서만 검증됐고 상점/학습 등록은 아직 아니다. 황금과 뱃사람 즉시공격은 보류한다. 재학습 전 새 실행본과 source/rules 서명을 고정한다. 기존009모델은 보존된 고정 실행본에서만 재현한다.
