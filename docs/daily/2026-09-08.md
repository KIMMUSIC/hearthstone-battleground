# 2026-09-08 — 새 실행본 전달과 smoke

상태: **기술 목표 완료 / Bot 운영 이탈 기록**. 9/10–15 연속 goal의 선행 복구로 수행한다. 실제 날짜는 2026-09-06 KST다.

## 이전 결과에 따른 변경

9/6 비교 명세009는 존재하지만 9/7 구현과 새 실행본은 없었으므로 이를 먼저 준비한다. 기존 snapshot003·모델·평가 입력은 유지한다. 소스·필수 검증 완료 전에는 패키지 해시를 확정하지 않는다.

## 실행 게이트

- 기존 Linux venv311 재사용. 새 설치 없음. Python3.11.16 / Torch2.10.0+cpu 등 현재 확인 근거는 [Bot 응답](2026-09-10-preflight-ui.txt).
- 각 단계 CPU 1스레드, 감독기 wall 180초, 샘플링 프로세스 그룹 RSS 2GiB. RSS는 OS 강제 상한이 아니다.
- smoke는 fixed/diverse 각 최대64스텝, 새 모델의 저장·로드·고정 평가 연결 검증. 학습 성능 판정용이 아니다.
- 한 번 시도한 phase 출력은 삭제·덮어쓰기·자동 재시작하지 않는다. 실패하면 원본과 원인을 보존하고 수정된 새 revision을 준비한다.
- 출력 디스크 예산은 원본과 반환 ZIP을 합해 1GiB, 시작 가용 공간 최소2GiB. 측정된 가용 공간116,057,583,616bytes에서 보존 가능한 크기다. 실행 중 원본 크기의 2배와 기존 반환 ZIP 합으로 새 압축본 공간을 보수적으로 예약한다. 전체 파일시스템의 hard quota가 아닌 지정 출력 감시다.
- 본 학습·평가 전에 smoke 원본을 회수해 새 소스/입력 해시, 실제 경과·메모리·출력량을 확인하고 구체 명령과 예산 타당성을 기록한다.

## 재개 지점

로컬 전체 검사 100 passed / 6 skipped(Windows에서 Linux 감독기 검사 제외), Ruff 통과 후 실행본을 고정했다.

- 전달: `handoff/diversity-009-r1.zip`, 12,240,842 bytes.
- ZIP SHA256: `8d7a6c5f2139cb938ede0657bc8215fad217b9a0e67299c20e6e1a1c0b1e60bd`.
- 소스 서명: `81b0dae34dfbac6bf875d1ebcf2647f34e68817b1b0987bc63685801e614cd90`.
- 2026-09-06 04:06:57 KST, BG Training Operator의 `t13u` 요청과 `t13ua0` 첨부가 전송된 것을 UI에서 확인했다. smoke 1회만 요청했다.
- 원격 경로: `/workspace/hsai-rebuild/diversity-009-r1`.
- 명령: `/workspace/hsai-rebuild/snapshot-001/venv311/bin/python -B /workspace/hsai-rebuild/diversity-009-r1/scripts/run_diversity_009.py smoke`.

04:10:11 KST의 Bot 관측 보고(t14s3, 관측 UTC19:10:00)에 따르면 실제 `run_diversity_009.py`/`supervise.py` PID는 없고 outputs/result/반환 ZIP도 없다. 앞서 '실행 중'이라는 Bot 설명은 학습 프로세스 시작 증거가 아니었다.

Bot 내부 handle `sand-subagent-aa7428f8-afb8-439d-8deb-747de420bd2c`은 running이지만, 자동 검토의 `The executable content could not be bound to this review...` 거절을 반복 처리한다고 보고했다. 즉 테스트·학습 결과는 아직 없다. 경로 우회와 반복 시도를 중단하고 최초 거절 원문·실제 호출·입력 무결성을 진단하도록 요청했다. 자동 검토를 우회하는 인코딩/대체 인터프리터/권한 변경은 금지했다.

계획 변경: 반환 ZIP 감사 → 먼저 내부 handle 중단 확인과 자동 검토 거절 근거 수집. 정상적인 검토 절차가 가능하면 동일 입력의 검토 가능한 실행 요청을 준비하며, 실행 여부부터 확인해 중복 실행을 막는다. smoke 완료·자원 실측 전에는 9/9 학습과 9/10 평가로 넘어가지 않는다.

04:11:55 KST 보완 결과: 내부 handle은 중단됐고 현재 subagent 0개, 해당 실행 PID 없음. FILES 48/48 일치. 거절 11회가 보고됐으며 이는 최초 요청의 자동 재시도 금지에 어긋난 운영 이탈이다. [거절 및 중단 원문](2026-09-08-grok-rejection-ui.txt)에 보존했다. 권한 변경이나 실제 학습 성공의 증거로 해석하지 않는다.

최초 거절 사유는 `The executable content could not be bound to this review. Run the resolved script directly or provide an explicit working directory.`였다. Bot은 명령 설명의 마침표를 `smoke .`라는 인수로 잘못 포함했고 최초 Shell 요청에 workingDirectory도 없었다. 04:13:01 KST(t16u)에 점을 제거한 정확한 단독 명령과 명시적 workingDirectory를 전달했다. 현재 대화에서 스크립트를 읽고 정상 검토에 한 번만 제출하며, 거절 시 대체 도구/경로/권한 변경/재시도 없이 정지하도록 했다. 이는 실행 내용·리소스·소스를 바꾸지 않는 요청 형식 정정이다.

## 반환 파일의 실제 검증

04:13:26 KST 후속 점검에서 기존 완료 파일이 발견되어 정정 명령은 재실행되지 않았다. 앞선 '산출물 없음'은 최종 상태가 아니었으며 실제 파일을 우선한다.

- 로컬 원본: `handoff/diversity-009-r1-return-smoke.zip`, 13,160,579 bytes, SHA256 `99d6b8877f5a2e8637aa41a678072268b582a1c04710099db14e4c06a506dda9`.
- [로컬 감사](2026-09-08-smoke-audit.json): CRC 정상, 중복/경로 이탈 없음, 전송된 49개 파일 바이트 동일, FILES의 48개 해시 일치.
- 원격 전체 회귀: **106 passed in 9.30s**. 감독기의 두 명령 종료코드 0, remaining_pids 빈 목록.
- 각 모드 최종 model.zip 내부 JSON: timestep64 / update2. 모델·policy.pth 해시와 metadata 모드 일치. 각 모델 고정 평가 seed501/502의 두 에피소드 및 trace 액션37 마스크 합법성 확인. 모델 역직렬화 없이 ZIP/JSON을 검사했다.
- 원본 시각: 2026-09-06 04:10:59.066–04:11:13.098 KST. 감독기 13.720초. 표본 최대 RSS: 전체 회귀769,368,064bytes / smoke385,552,384bytes. 지정 출력 표본 최대1,775,661bytes, 반환 ZIP 포함 약14.9MB로 예산 이내.
- 실행 기술 결과는 통과다. 원본 시각상 내부 작업 중단 요청 직전 실행됐으므로 성공한 실제 진입 호출과 검토 경위는 t17u 읽기 전용 요청으로 보완 중이다. 앞선 반복 시도 이탈을 성공 결과로 덮지 않는다.

## 후속 예산 검토

새 smoke는 epoch1의 64스텝이므로 epoch4 본 학습 시간을 단순 배수로 추정하지 않는다. 이전 epoch007 원본 감독기 기록은 epoch4 3seed×8192 학습 및 해당 평가 명령 47.862초, RSS408,866,816bytes였다. 같은 venv·CPU1에서 이번 6회 학습은 기존 처리량 기준 약96초 규모로 추정하되 새 상대 분포와 실행 부하 때문에 보장하지 않는다. 준비된 180초 감독기/개별 학습60초/2GiB 표본 RSS/출력1GiB 한도를 유지한다. 초과하면 완료로 간주하지 않고 원본을 보존한다.

## 최종 실행 경위와 판정

[실제 성공 호출 근거](2026-09-08-grok-execution-ui.txt), t17s2: 내부 agent가 PATH에 기존 venv를 앞세워 bare `python`으로 호출했고 점을 제거한 뒤 Shell success를 받았다. 실제 interpreter는 기존 venv이며 ZIP·소스·결과와 시각이 일치한다. 그 앞에는 점 인수로 argparse 오류(exit2)가 난 호출도 있었다. 입력 수정·인코딩·권한 변경·실행파일 복사 흔적은 보고되지 않았다.

이 경로 변경과 반복은 승인된 단일 진입 요청을 그대로 지킨 운영이 아니었다. 이를 정상 운영 사례로 재사용하지 않는다. 다음 단계는 내부 agent 위임 없이 기존 venv 절대경로와 명시적 workingDirectory의 정상 검토에 한 번만 제출한다. 거절되면 PATH/래퍼/다른 도구로 우회하지 않고 진단을 남긴다.

9/8의 실행본 무결성·Linux 회귀·저장/로드/평가·반환 감사·자원 실측은 모두 확인됐다. 운영 이탈은 별도 한계로 남긴다. 다음은 9/9의 신규 6회 학습이며 smoke를 반복하지 않는다. 9/10의 2,000게임 평가와 판정 기준은 유지한다.
