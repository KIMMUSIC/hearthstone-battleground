# 2026-09-23 — 확대 보류와 제한 실험 선택

상태: **완료**. 분기와 실행 명세를 고정하고 할인율 설정 회귀를 검증했다. 실제 작업일 2026-09-06.

[021 감사](../../runs/campaign-021-local-r2/audit.json)는 실제 300,000스텝과 900게임, 39개 학습 구간·48개 감독 단계를 통과했다. 평균 초기 대비 보상 증가 1.96, 양수 3/3, 생존율 증가 0.7233으로 학습 진행 기준을 충족한다. 그러나 heuristic 대비 최종 평균 보상 차이 -7.44는 사전 기준 -0.5보다 낮다. 따라서 100만 스텝 확대를 보류한다. 판정 기준은 변경하지 않았다.

[9/24–25 고정 명세](../../experiments/DISCOUNT_024.md)는 gamma 0.99와 1.0만 비교하는 새 학습 6회, 각 8,192스텝이다. 두 조건 모두 동일한 새 소스에서 시작하므로 이전 모델의 결과를 수정 후보의 성능으로 사용하지 않는다. 개별·누적 자원 한도와 후보 판정 기준을 실행 전에 명시했다.

할인 반례는 같은 전투 결과를 더 많은 무보상 행동 뒤에 받으면 할인된 손실이 작아지는 목적함수 문제를 보인다. 021의 최종 모델에서는 동결과 강제 종료가 0이어서 이 문제가 현재 격차의 주원인이라는 증거는 없다. 단일 가설의 제한 실험으로만 취급한다. 나쁜 결과가 나오면 학습량을 자동 확대하거나 다른 가설을 연달아 시험하지 않는다.

`src/hearthstone_ai/training.py`는 gamma 기본값0.99를 유지하고 유한한 0<gamma<=1만 허용한다. PPO에 값을 명시적으로 전달하고 재개 시 checkpoint 설정과 다른 gamma를 모델 로드 전에 거절한다. 과거 metadata에 gamma가 없으면0.99로 해석한다. `tests/test_training.py`는 실제32스텝 gamma1 학습·저장·로드, metadata, 잘못된 값, 재개 불일치 거절을 포함한다. WSL 전용 Python에서 해당 파일 **23 passed in 4.20s**, 두 변경 파일 Ruff **All checks passed**. 설정 변경 자체로 성능 향상을 주장하지 않는다.

다음: 9/24 실행기를 검증하고 별도 입력 패키지를 고정해 명세 실행을 진행한다. 후보 없음도 9/26의 유효한 결론이며, 엔진 검증 이후 9/27–10/1의 8인 엔진 개발은 계속한다.

추가 검증: `tests/test_discount_objective.py`에서 세 seed의 실제 게임 반례를 회귀로 보존했다. `scripts/discount_decision.py`와9개 테스트는 paired 비교·기준선·초기 대비 개선·생존율·누락/비정상 지표·좋은 seed 하나만 고르는 오류를 검사한다. 구현 전 해당 테스트의 실패를 확인하고 구현 후 통과했다. 9/24 실행기 추가 전 현재 전체 WSL 검증은 `python -B -m pytest -q -p no:cacheprovider` **200 passed in 13.48s**, `ruff check src scripts tests` **All checks passed**, 종료 코드0이다. 별도024 캠페인은 아직 실행하지 않았다.
