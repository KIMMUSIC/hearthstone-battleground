# 10/2 이후 실행 계획

날짜는 작업 순서다. 사용자의 후속 연속 진행 요청에 따라 선행 검증을 통과한 다음 작업을 이어서 수행한다. 원본 프로젝트는 읽기 전용이며 로컬 WSL 전용 환경·CPU1·개별 학습60초·감독180초·샘플링RSS2GiB·출력+반환1GiB 한도를 유지한다.

## 이번 결과가 바꾼 우선순위

032는8,192스텝/1,024업데이트, 학습13.39초(약612스텝/초),60평가와2재현 및 독립 감사가 통과했다. 하지만 학습 중 순위가 확정된138에피소드 모두8위였고, 개발20seed에서 초기·최종 모두8위다. 최종 정책은2,880행동 모두 동결이었으며 구매·배치가0회였다. 동일 조건의 heuristic 평균은4.25위다. [10/2 결과](daily/2026-10-02.md)를 근거로 **무조건 학습량 증액보다 보상 신호와 상대 난이도 진단을 우선**한다. 처리량 부족을 주된 원인으로 단정할 근거는 없다.

## 10/3 — 산출물 감사와 다음 실험 고정

완료: [4주 감사](daily/2026-10-03.md).

032의 실제 스텝·업데이트·처리량·평가와 021/024/030의 고정 증거를 연결한 보고서를 만든다. 모델을 어떤 환경에서 사용할 수 있는지 명시하고, 단일 seed smoke와 성능 검증을 구분한다. 후속 실험의 seed·상대·예산·판정 기준을 실행 전에 고정한다. 새 대규모 학습은 하지 않는다.

## 10/4 — 8인 학습 행동 진단과 평가 설계

완료: [034 진단](daily/2026-10-04.md). 240에피소드와127,343행동 감사가 통과했고3heuristic/4random 가설이 사전 기준을 충족했다.

저장된 초기·최종 trace에서 구매·배치·판매·리롤·동결·강제 종료를 비교한다. 순위 보상의 희소성, 관측 크기/스케일, 좌석0 편향을 각각 가능한 원인으로 구분하고 가장 근거 있는 한 가설만 선택한다. 모든 정책이 같은 조건을 사용하는 규칙 기반·무작위 기준선을 마련한다. 기존20001..20020은 개발 세트이며 최종 평가 세트로 재명명하지 않는다.

우선 검토할 가설은 **강한 규칙 기반 상대7명 때문에 초기 정책의 종국 순위가 모두8위여서 행동 품질을 구분할 신호가 부족하다**는 것이다. 현재138개 학습 결과가 근거이며 원인 확정은 아니다. 약한 상대를 일부 섞은 조건에서 순위 분산이 생기는지 짧은 규칙 기반/무작위 probe로 확인하고, 동결 반복과 정상 경제 행동이 동일 순위·보상으로 끝나는 반례도 기록한다. gamma1에서도 발생했으므로 할인율 변경만 재시도하지 않는다. 보상·상대·관측을 한 번에 바꾸지 않는다.

완료 기준: 대표 실패 trace, 한 가설과 반증 조건, 학습/개발/미사용 검증 조건, seed별 판정 규칙을 명세한다. 원인을 찾지 못하면 카드 추가나 예산 증액으로 대신하지 않는다.

## 10/5–6 — 제한된 다중 seed 학습 비교

완료: [10/5 학습](daily/2026-10-05.md), [10/6 평가](daily/2026-10-06.md). 고정 실행본 lobby-035-local-r1의 49,152스텝과 280평가 감사가 통과했다. 모든 seed의 공통 평균순위 개선이 0으로 후보 미채택이다.

10/4에서 가설을 지지하는 증거가 나온 경우에만 대조군과 한 변경 조건을 seed7·17·27로 비교한다. 최대2조건×3seed×8,192스텝이며 각60초 한도를 유지한다. 학습 상대가 달라도 개발 평가는 동일한 상대 구성으로 비교하고 학습 성적과 구분한다. 시간 중단이 생기면 실측으로 동일 학습량을 사전 재설정하고 새로운 실험 ID를 사용한다. 평균 순위·상위4위 비율·제한 중단·동결/구매/배치 비율을 함께 보고한다.

완료 기준: 모든 seed를 포함한 결과와 불확실성, 실제 업데이트·모델 불변·재현 감사. 개선이 없으면 진단으로 돌아가고 10만/100만 스텝을 자동 시작하지 않는다.

## 10/7 — 실패 원인 추가 진단

완료: [036 결과](daily/2026-10-07.md). 초기 정책의 Tanh 포화 평균59.42%, 학습 후76.86–80.05%로 관측 스케일 비교의 근거를 확보했다. 원인 확정이나 성능 개선 증거는 아니다.

035에서 상대 혼합만으로 공통 평가 개선이 없었다. 036은 저장된 6모델의 초기/최종 정책에 동일한 heuristic 관측 코퍼스를 입력해 필드별 스케일과 첫 Tanh 층의 포화 비율을 측정한다. 실제 gamma·GAE lambda·rollout 길이와 종국 신호의 거리별 감쇠도 함께 확인한다. 모델 학습·수정 없이 기존 개발 seed만 사용한다. 활성 포화는 원인 확정이 아니며, 반사실 입력이나 해석 수치를 경기 성능으로 표현하지 않는다.

결과에 근거해 다음 비교의 단일 변경과 반증 기준을 먼저 고정한다. 근거가 부족하면 더 좁은 진단으로 이어가며 동일 실험의 자동 재시도나 학습량 증액은 하지 않는다.

## 10/8 이후 — 진단에 따른 비교, 그 뒤 카드 확대

10/8 완료: [037 결과](daily/2026-10-08.md). 초기 포화는59.42%→0.0214%로 감소했지만 공통 순위 개선0으로 성능 후보 미채택이다.332테스트,49,152스텝,280평가와23,615행동 감사 통과.

10/10의 [039 rollout32/128 비교](daily/2026-10-10.md)를 완료했다. 평균순위 개선0.0833·개선seed1/3으로 미채택이다.49,152스텝·280평가·26,967행동 및 실제Adam/SB3 카운터 감사 통과.

10/11 완료: [040 관측 정보 충분성 진단](../experiments/LOBBY_040_INFORMATION_DIAGNOSIS.md)에서 160경기·11,435상태의 행동 복원 불일치가 0건이었다. 10/12 완료: [041 모방 학습 진단](../experiments/LOBBY_041_IMITATION.md)에서 학습/개발 trajectory를 분리하고 3개 seed의 행동 일치율과 자기 행동 경기 순위를 별도로 검증했다. 두 기준 통과 시에만 모방 초기화와 PPO 연결 비교를 명세한다. 일치율만 통과하면 행동 경로 차이를, 일치율도 미달하면 행동 분류 오류를 우선 진단한다. 하이퍼파라미터만 계속 바꾸거나 학습량을 자동 증액하지 않는다.

041 실제 결과: 개발 일치율79.25–79.56%, 평균순위 개선0.975, 상위4위5%로 두 사전 기준 미달이다. [10/12 결과](daily/2026-10-12.md)에 따라 PPO 연결 비교를 보류하고 10/13 완료: [042 행동별 오류 진단](../experiments/LOBBY_042_CLASSIFICATION_DIAGNOSIS.md)을 진행한다. train/dev 혼동 행렬, 행동별 오류 기여도, 자기 방문 상태에서의 teacher 일치율을 읽기 전용으로 비교한 뒤 다음 단일 변경을 고정한다.

042 최종 v2에서 개발 macro32.79–33.48%, 자기 방문 상태 teacher 일치율38.21%, 세 모델 공통 오류441/2,236행을 확인했다. 10/14 완료: [043 행동 균형 샘플링 비교](../experiments/LOBBY_043_BALANCED_IMITATION.md)다. 같은 업데이트 예산에서 샘플 확률만 비교하고 실제 인덱스와 개발 macro 분모를 고정했다. 041의 기존 기준은 유지하며 043의 새 비교 기준을 소급 적용하지 않는다.

043 감사가 통과했으나 분류 기준만 지지됐다. macro13.98%p 개선과 달리 순위개선0.175위·양성seed1/3·상위4위 감소로 미채택이다. 10/15 완료: [044 실제 방문 상태 진단](../experiments/LOBBY_044_VISITED_STATE_DIAGNOSIS.md)이다. 최종120경기에서 모델예측을 재현하고 teacher 일치율·행동별/라운드별 오류·자기 상태 분포를 비교한다. 개발 방문 상태는 학습에 사용하지 않으며, 차이가 남으면 학습seed만으로 방문 상태를 수집하는 별도 비교를 명세한다.

044에서 후보3seed 모두teacher dev 대비 실제 방문 상태 일치율이20%p 이상 낮았다. 120경기·7,909행동 재현과 입력 불변을 확인했다. 10/16 완료: [045 학습 방문 상태 추가 비교](../experiments/LOBBY_045_VISITED_TRAINING.md)의 구현·실행·독립감사를 마쳤다. 수집은 학습seed0..99만 사용하며 개발 방문 상태를 학습에 넣지 않는다. 새 모델6개의 같은 업데이트 예산에서 학습 데이터 추가만 비교한다.

카드 확대는 035 실패로 선행 조건 미충족이다. 후속 단일 변수 비교와 공통 평가에서 학습 경로가 검증된 뒤 아래 작업을 진행한다. 고정 완료 날짜보다 검증 조건을 우선한다.

학습 경로와 평가가 안정되면 상점7종에서 우선2–3종 추가 후보를 조사한다. 기존 지원 효과를 활용할 수 있는 후보를 먼저 검토하고, 새 효과는 일반/황금/트리플/발견/전투 상호작용까지 검증할 수 있을 때만 넣는다. 기준 규격과 공식 출처·카드 ID를 확인한 뒤 실제 수를 확정한다. 토큰과 중복 ID를 별도 카드 수로 부풀리지 않는다.

완료 기준: 후보별 지원 표, 고정 ID 매핑과 새 환경 서명, 풀 보존·관측·경제·전투 회귀, 규칙 기반 경기 완주, 새 버전의 짧은 학습. 기존 모델은 호환 검사 없이 재사용하지 않는다. 카드 수 목표보다 효과 정확성을 우선한다.

## 그 이후의 조건부 순서

1. 좌석과 상대 정책을 다양화하고 보지 않은 조건에서 평가한다.
2. 영웅 능력은 별도 효과 계약과 회귀를 갖춘 작은 묶음으로 도입한다.
3. 처리량과 학습 추세가 뒷받침될 때만 학습량 증액을 명세한다.
4. 실제 게임 상태 입력·추천 연결은 규칙 차이, 입력 정확도, 추론 지연을 검증한 뒤 다룬다.

전체 카드·영웅·최신 패치 재현·완전 자기대전의 완료 날짜는 아직 정하지 않는다. 각 단계 종료 시 실제 결과에 맞춰 이 계획을 갱신한다.


## 10/17 — 데이터 출처별 오류 진단 (완료)

045 결과 평균순위6.90→7.00위·개선seed1/3·후보2개의teacher dev정확도75%미달로후보미채택이다. 학습데이터증강이항상유리하다는가정을두지않는다. [10/16 결과](daily/2026-10-16.md)와 [046 확정명세](../experiments/LOBBY_046_POST_COLLECTION_DIAGNOSIS.md)를 따른다.

새 학습 없이같은6개최종모델을teacher기본학습/collector추가학습/dev코퍼스에비교한다. 출처별micro/macro/NLL과기존최종120경기의라운드·종류/슬롯오류를구분한다. 기본행정확도하락과추가행개선의동시발생→추가행학습실패→남은방문상태격차순으로사전분기한다. 조건이없으면증거부족을기록한다. 단일후속가설은이결과후명세하며PPO연결·카드확대·예산증액은현재범위에넣지않는다.

## 10/18 — 기본 데이터 비중 고정 비교 (재개 대기)

046완료: 18분류·120기존경기재현에서2/3seed의추가상태정확도개선과기본상태저하를확인했다. 나머지seed는양쪽저하이며동일오류쌍집중조건도미충족이다. [10/17 결과](daily/2026-10-17.md).

[047 확정명세](../experiments/LOBBY_047_SOURCE_MIX.md)에따라기존증강데이터를유지하고기본48/추가16샘플quota를한번비교한다. 출처내확률은기존확률의조건부분포다. 같은모델/예산/초기가중치조건과045대조군재현을선행검증한다. 실패시다른비율을자동탐색하지않으며기존채택기준은보존한다.

사용자 종료 요청 반영: 046 결과와047 계획 정리까지 완료하고 실행을 종료했다. 047은 구현·학습·평가 모두 미착수이며 사용자의 재개 요청 이후 진행한다.
